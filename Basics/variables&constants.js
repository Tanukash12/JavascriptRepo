const a = 10;
// const is constant which can't be change further 

let b = 45;
// it is scope based variable 
// it can be change 

var c = 60
// do not use often 
// no scope
// not to use recommended!

console.table([a, b, c]);

// Possiblity : initailizing a variable without datatype
city = "Jaipur"

console.log(typeof city);

// if we only declare do not give it a value then the datatype is "undefined"

let place;
console.log(typeof place);